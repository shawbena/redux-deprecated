function connectAdvanced(selectorFactory: any, {
	getDisplayName = (name: string) => `ConnectAdvanced(${name})`,
	methodName = 'connectAdvanced',
	renderCountProp = undefined,
	storeKey = 'store'
}){
	
}
